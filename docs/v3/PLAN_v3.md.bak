这是一个容器化项目，但是只支持到Android12，现在需要你以支持Android13、14版本。

如果你对于当前项目还不了解，可以先通览整个项目，了解其技术路线，实现方案等等。

显著我找了一些需要容器化的应用和设备，你通过在设备上测试，观察运行情况，决定修修改方向。目的是通过这些应用的运行失败，来帮助你知道，如何让容器内的环境更像真实的设备。

目前测试设备是Android API 35

# 必须遵守的准则

- 必须先尝试编译并安装应用，如果不成功，不进行任何修复操作，全力解决编译问题，要求不在沙盒里编译应用
- 目标测试应用里有时候会出现一些异常或者文件找不到，是正常的
  - 在无法确定某些情况是否来自于容器化本身时，你需要对比在**真实环境和容器化环境里的日志**，来确定某个异常或者日志，是否来自于容器化方案本身的问题
  - 不要假定所有的异常、错误、文件找不到等等都是来自于容器化的问题
  - 如果容器化的日志可以明确来自于容器化本身，则不要再次获取真实环境的日志
- **不要试图反编译目标应用**，这会让你陷入陷阱，为了让目标应用运行起来，而做针对性的修改，这是一款容器化的应用，我们的目标是让容器内的环境尽量贴合真实的系统，因此目标应用在真实设备可以运行，就一定可以有办法在容器化内运行。
- 你做的每一步都要有依据，并且必须交叉验证，得到肯定答复后，再实施编码
- 当本地信息不足无法交叉验证时，**必须联网搜索**
- 所有对于代码的修改，只要是因为要模拟系统环境的，都**必须联网搜索**核对AOSP代码
- 项目不要，也不允许单元测试
- 实施过程中，阶段性的成功，应当保存在docs/v3目录，时刻需要核对文档与当前进度，同时文档里应当记录当前的问题和处理思路，即使是验证过，是错误的思路也应当记录，避免以后再次出现同样的错误。
  - 当某些阻塞项或者问题已经被解决时，也需要在文档里记录
  - 待处理事项需要在文档里记录
  - 每次验证失败时，需要再次核对文档，修正处理方向，必要时，搜索网络获取帮助
- 对于本项目的修改，**必须确保不破坏原本的兼容性**，不能为了支持高版本Android，而导致原本可以正常运行的低版本Android无法运行
- 如果你发现某些代码修改的有错误，可以对比git的历史记录，进行回滚
- 所有hook的framework、NDK位置，都需要核对AOSP代码，不允许猜测和盲写

# 环境

- 编译项目需要使用Java 11版本，可通过下面的命令
  - source "$HOME/.sdkman/bin/sdkman-init.sh"
  - sdk use java 11.0.14.1-jbr
- 编译应用**无需在沙盒里**，避免不必要的环境配置和检查
  - 如果此前有记录编译应用失败，则需要**再次在真实环境尝试**，以确认此前的记录是否正确
  - GRADLE_HOME不需要指定，使用默认即可

# 测试步骤

- 本次要测试的包名com.bestv.tv.video.iqy.tjdx
- 使用Java 11编译项目
  - `./gradlew assembleBlackBox32Debug`
- 使用adb命令安装到设备上，如果存在多个可用设备，可以任意选择一个，环境是一致的
- 当确定测试包名后，启动app项目，传入测试的包名（不可以固定在代码里），并进行下列步骤
- `adb shell logcat -c`清空原本的日志
- `adb logcat` 获取日志，需要在运行前获取，防止由于设备缓冲区不够，而丢失日志
- 启动应用通过`adb shell am start <component_name>`
  - 这里应该启动app项目里的WelcomeActivity，并传入相关`FLAG_TEST`和`TEST_PACKAGE`参数，以达到直接测试容器化框架的目的
  - 测试的应用在容器化内与设备物理上均以安装
- 等待30秒，查看获取到的日志进行分析
- 通过观察logcat日志来判断目标应用是否启动/运行成功，如果失败，则根据日志信息排查
  - **禁止**通过grep或者关键字过滤查看日志，这会让你漏掉一些关键信息或者系统本身输出的有价值日志
  - **必须**逐行阅读每一行日志
  - 查看日志需要`logcat_explorer`子Agent来完成，如果因额度用尽，也可以在当前会话里审阅日志
  - 对于无法确定边界或怀疑是应用本身的问题，可以在物理环境运行这个应用（已安装）对比日志
- 通过截图，进一步判断目标应用是否运行正常
  - 注意：当且仅当logcat日志正常时，才进行截图判断进行兜底，如果日志中有显著异常、错误信息，不需要截图
- 对于日志、截图需要临时保存到本地的，文件名**应当固定**，禁止每次调试都保存不同的文件名
- 对于需要执行多个命令或者复杂命令的，应当将命令内容写入/编辑到项目目录下的script/codex.sh，然后执行这个脚本，我已经给予这个文件可执行权限，无需重复

# 验收标准

- 阅读日志时，不要有选择性的阅读，例如通过grep只查看部分，要通读**全部内容**，防止遗失关键信息
- 截图仅当日志里未发现显著错误时，才需要
- 一些测试应用可能采用了一些第三方的加固，不要试图对这些加固方案做**针对性的处理**，而需要考虑容器化环境下有什么和正常安装时不一样的，必要时对比AOSP源代码，要从系统层面，全局考虑，为什么在容器里会有问题，而直接在系统上就是正常的，着重对比差异点，来解决问题，而不局限于某个特定壳或者加固方案，不要被他的错误消息误导。
- 困惑时，不要猜测，尽量联网搜索，多看别的容器化框架是如何做的
- 对于本项目的修改，也需要尽量符合本项目原本的代码风格。保持修改后的代码，仍然易于阅读。
- 所有修改完成，且验收通过时，最后再做一次全面复盘：
  - 检查每一个修改，是否都有必要，是否有更好的写法，是否冗余，是否会对其他版本的Android产生副作用
  - 如果不能确定答案，则联网搜索
  - 当明确有改进空间时，则改进代码，随后再次全面复盘
- 当本次修改通过验收，且全面复盘后，无意义，最后产出一份详尽的文档，需要载明：
  - 每一处修改的内容
  - 为什么要修改
  - 为什么要这样改
  - 这样修改后有什么后果
  - 如果对应的修改有参考来源，需要步骤来源，如果来自于互联网，需要把对应资料的网址写上